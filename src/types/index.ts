/**
 * Central export for shared types.
 * Import from "@/types" or "@/types/metric" as needed.
 */

export type {
  Metric,
  CreateMetricPayload,
  MetricsQueryParams,
  MetricSummaryStats,
  BreakdownDimensionId,
  BreakdownDimensionConfig,
} from "./metric";
